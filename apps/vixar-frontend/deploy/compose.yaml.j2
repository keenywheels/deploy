services:
  {{ app.name }}:
    image: {{ app.image.name }}:{{ app.image.tag }}
    container_name: {{ app.name }}
    restart: always
    mem_limit: 512m
    ports:
      - '3000:3000'
    networks:
      - {{ app.name }}-net

networks:
  {{ app.name }}-net:
    external: true
